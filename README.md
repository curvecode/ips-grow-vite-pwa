# ips-grow-vite-pwa Project Workspace

This repository contains a collection of projects and submodules. Below is an overview of the directories and their respective purposes based on their configurations.

## Project Structure

### 1. `api/`
- **Description:** Daily Buy API Server
- **Tech Stack:** Node.js, Express, TypeScript, TSX
- **Scripts:** 
  - `dev`: Runs a watch mode server via `tsx`
  - `build`: Compiles TypeScript via `tsc`
  - `start`: Runs the compiled server from `dist/server.js`

### 2. `ui/`
- **Description:** A front-end user interface application
- **Tech Stack:** Vite, TypeScript (Packaged with bun)
- **Features:** Supports different build modes (development, staging, production) and includes `rollup-plugin-visualizer`.

### 3. `ang-shared-header/`
- **Description:** Angular project (Application/Library)
- **Tech Stack:** Angular (v19.2), RxJS, TypeScript, Karma (Testing), Jasmine
- **Scripts:** Standard Angular CLI commands (`start`, `build`, `test`, `watch`).

### 4. `common/`
- **Description:** A shared package containing data models and utilities.
- **Key Files:** `daily-buy.model.ts`, `index.ts`
- **Purpose:** Likely used to share types across the `api` and `ui` packages to ensure type safety between frontend and backend.

### 5. `react-course/`
- **Description:** A directory containing course or tutorial files.
- **Key Files:** `index.html` 

---
*This README was automatically generated by scanning the workspace directories.*
